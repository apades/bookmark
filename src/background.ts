import { wait } from './utils'

console.log('background')
;(async () => {
  // await wait(1000)
  // chrome.bookmarks.getTree((res) => {
  //   console.log('res', res)
  //   chrome.runtime.sendMessage({
  //     type: 'bookmarks:datalist',
  //     data: res,
  //   })
  // })
})()
